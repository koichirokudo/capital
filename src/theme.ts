import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  // theme
  palette: {
    primary: {
      main: '#ffa500',
    },
    secondary: {
      main: '#ffb6c1',
    },
  },
})

export default theme
